<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="balgil.com.trip.payment">
	<insert id="PAYMENT_INSERT" parameterType="balgil.com.trip.payment.model.PaymentVO">
		insert into payment (id, price_total, coupon, point, price_final, res_id)
		values (seq_reservation.nextval, #{price_total}, #{coupon}, #{point}, #{price_final}, #{res_id})
	</insert>
</mapper>